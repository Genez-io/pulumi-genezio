name: provider-genezio-native
runtime: yaml
plugins:
  providers:
    - name: genezio
      path: ../../bin

resources:
  myRandomResource:
    type: genezio:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
